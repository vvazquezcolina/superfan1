---
phase: 02-base-layout-technical-seo
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/sitemap.ts
  - src/app/robots.ts
  - src/lib/seo.ts
  - src/app/[lang]/ciudades/[slug]/page.tsx
  - src/app/[lang]/estadios/[slug]/page.tsx
  - src/app/[lang]/equipos/[slug]/page.tsx
  - src/app/[lang]/page.tsx
autonomous: true
requirements: [SEO-01, SEO-02, SEO-04]

must_haves:
  truths:
    - "/sitemap.xml returns a valid XML sitemap listing all pages with correct priorities and hreflang"
    - "/robots.txt returns valid directives allowing all search engine and AI crawlers"
    - "Every page has a unique title following the '{Page Title} | SuperFan Mundial 2026' format"
    - "Every page has a meta description between 120-155 characters"
    - "Every page has canonical URL, Open Graph tags, and Twitter Card meta tags"
    - "AI crawlers (GPTBot, ClaudeBot, PerplexityBot) are explicitly allowed in robots.txt"
  artifacts:
    - path: "src/app/sitemap.ts"
      provides: "Dynamic sitemap generation for all pages"
      exports: ["default"]
    - path: "src/app/robots.ts"
      provides: "Programmatic robots.txt"
      exports: ["default"]
    - path: "src/lib/seo.ts"
      provides: "Shared metadata helper for OG, Twitter Cards, and canonical URLs"
      exports: ["buildPageMetadata"]
  key_links:
    - from: "src/app/sitemap.ts"
      to: "src/lib/content/cities.ts"
      via: "import data loaders to enumerate all pages"
      pattern: "import.*getCities.*from.*content/cities"
    - from: "src/app/[lang]/ciudades/[slug]/page.tsx"
      to: "src/lib/seo.ts"
      via: "import metadata helper"
      pattern: "import.*buildPageMetadata.*from.*lib/seo"
    - from: "src/app/robots.ts"
      to: "src/lib/i18n.ts"
      via: "import SITE_URL for sitemap reference"
      pattern: "import.*SITE_URL.*from.*lib/i18n"
---

<objective>
Implement the full technical SEO baseline: programmatic sitemap.ts with all pages and hreflang alternates, robots.ts allowing all crawlers including AI bots, and enhanced generateMetadata on every page with canonical URLs, Open Graph, and Twitter Cards.

Purpose: Search engines and AI crawlers must discover, understand, and properly index every page. Without sitemap, robots, and proper meta tags, the site is invisible to Google, ChatGPT, Perplexity, and Claude.
Output: sitemap.ts, robots.ts, seo.ts helper, and updated page metadata across all existing pages.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-base-layout-technical-seo/02-CONTEXT.md
@.planning/research/PITFALLS.md

<interfaces>
<!-- Data loaders that sitemap needs to import -->

From src/lib/content/cities.ts:
```typescript
export function getCities(): City[]
export function getCitySlugs(): Array<{ slug: string; lang: Locale }>
```

From src/lib/content/stadiums.ts:
```typescript
export function getStadiums(): Stadium[]
export function getStadiumSlugs(): Array<{ slug: string; lang: Locale }>
```

From src/lib/content/teams.ts:
```typescript
export function getTeams(): Team[]
export function getTeamSlugs(): Array<{ slug: string; lang: Locale }>
```

From src/lib/content/schemas.ts:
```typescript
export type City = { id: string; slugs: { es: string; en: string }; name: { es: string; en: string }; country: 'mexico'|'usa'|'canada'; description: { es: string; en: string }; stadium: string; coordinates: { lat: number; lng: number }; lastUpdated: string }
export type Stadium = { id: string; slugs: { es: string; en: string }; name: { es: string; en: string }; city: string; capacity: number; coordinates: { lat: number; lng: number }; description: { es: string; en: string }; lastUpdated: string }
export type Team = { id: string; slugs: { es: string; en: string }; name: { es: string; en: string }; confederation: string; group?: string; description: { es: string; en: string }; lastUpdated: string }
export type Locale = 'es' | 'en'
```

From src/lib/i18n.ts:
```typescript
export const SITE_URL = 'https://www.superfaninfo.com'
export const pathTranslations: Record<string, Record<Locale, string>> = {
  ciudades: { es: 'ciudades', en: 'cities' },
  estadios: { es: 'estadios', en: 'stadiums' },
  equipos: { es: 'equipos', en: 'teams' },
}
export function buildAlternates(section: string, slugs: Record<Locale, string>)
export function buildIndexAlternates(section: string)
export function buildHomeAlternates()
```

Existing page metadata pattern (from src/app/[lang]/ciudades/[slug]/page.tsx):
```typescript
export async function generateMetadata({ params }): Promise<Metadata> {
  // Already has title, description, alternates with hreflang
  // Needs: canonical, openGraph, twitter additions
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create sitemap.ts, robots.ts, and seo.ts metadata helper</name>
  <files>
    src/app/sitemap.ts,
    src/app/robots.ts,
    src/lib/seo.ts
  </files>
  <read_first>
    src/lib/i18n.ts,
    src/lib/content/cities.ts,
    src/lib/content/stadiums.ts,
    src/lib/content/teams.ts,
    src/lib/content/schemas.ts,
    src/app/[lang]/layout.tsx
  </read_first>
  <action>
**1. Create `src/app/sitemap.ts`** -- Dynamic sitemap using Next.js built-in API (per D-16):

```typescript
import type { MetadataRoute } from 'next'
import { getCities } from '@/lib/content/cities'
import { getStadiums } from '@/lib/content/stadiums'
import { getTeams } from '@/lib/content/teams'

const SITE_URL = 'https://www.superfaninfo.com'

export default function sitemap(): MetadataRoute.Sitemap {
  const cities = getCities()
  const stadiums = getStadiums()
  const teams = getTeams()

  const entries: MetadataRoute.Sitemap = []

  // Homepage (both locales) -- highest priority
  entries.push(
    {
      url: `${SITE_URL}/es`,
      lastModified: new Date(),
      changeFrequency: 'weekly',
      priority: 1.0,
      alternates: {
        languages: {
          'es-419': `${SITE_URL}/es`,
          en: `${SITE_URL}/en`,
        },
      },
    },
    {
      url: `${SITE_URL}/en`,
      lastModified: new Date(),
      changeFrequency: 'weekly',
      priority: 1.0,
      alternates: {
        languages: {
          'es-419': `${SITE_URL}/es`,
          en: `${SITE_URL}/en`,
        },
      },
    }
  )

  // City pages -- high priority (main content)
  for (const city of cities) {
    entries.push(
      {
        url: `${SITE_URL}/es/ciudades/${city.slugs.es}`,
        lastModified: new Date(city.lastUpdated),
        changeFrequency: 'weekly',
        priority: 0.9,
        alternates: {
          languages: {
            'es-419': `${SITE_URL}/es/ciudades/${city.slugs.es}`,
            en: `${SITE_URL}/en/cities/${city.slugs.en}`,
          },
        },
      },
      {
        url: `${SITE_URL}/en/cities/${city.slugs.en}`,
        lastModified: new Date(city.lastUpdated),
        changeFrequency: 'weekly',
        priority: 0.9,
        alternates: {
          languages: {
            'es-419': `${SITE_URL}/es/ciudades/${city.slugs.es}`,
            en: `${SITE_URL}/en/cities/${city.slugs.en}`,
          },
        },
      }
    )
  }

  // Stadium pages -- high priority
  for (const stadium of stadiums) {
    entries.push(
      {
        url: `${SITE_URL}/es/estadios/${stadium.slugs.es}`,
        lastModified: new Date(stadium.lastUpdated),
        changeFrequency: 'weekly',
        priority: 0.8,
        alternates: {
          languages: {
            'es-419': `${SITE_URL}/es/estadios/${stadium.slugs.es}`,
            en: `${SITE_URL}/en/stadiums/${stadium.slugs.en}`,
          },
        },
      },
      {
        url: `${SITE_URL}/en/stadiums/${stadium.slugs.en}`,
        lastModified: new Date(stadium.lastUpdated),
        changeFrequency: 'weekly',
        priority: 0.8,
        alternates: {
          languages: {
            'es-419': `${SITE_URL}/es/estadios/${stadium.slugs.es}`,
            en: `${SITE_URL}/en/stadiums/${stadium.slugs.en}`,
          },
        },
      }
    )
  }

  // Team pages -- medium priority
  for (const team of teams) {
    entries.push(
      {
        url: `${SITE_URL}/es/equipos/${team.slugs.es}`,
        lastModified: new Date(team.lastUpdated),
        changeFrequency: 'monthly',
        priority: 0.7,
        alternates: {
          languages: {
            'es-419': `${SITE_URL}/es/equipos/${team.slugs.es}`,
            en: `${SITE_URL}/en/teams/${team.slugs.en}`,
          },
        },
      },
      {
        url: `${SITE_URL}/en/teams/${team.slugs.en}`,
        lastModified: new Date(team.lastUpdated),
        changeFrequency: 'monthly',
        priority: 0.7,
        alternates: {
          languages: {
            'es-419': `${SITE_URL}/es/equipos/${team.slugs.es}`,
            en: `${SITE_URL}/en/teams/${team.slugs.en}`,
          },
        },
      }
    )
  }

  return entries
}
```

Priority levels: homepage 1.0, cities 0.9, stadiums 0.8, teams 0.7 (per D-16 "proper priority levels").

**2. Create `src/app/robots.ts`** -- Programmatic robots.txt (per D-17):

```typescript
import type { MetadataRoute } from 'next'

export default function robots(): MetadataRoute.Robots {
  return {
    rules: [
      {
        userAgent: '*',
        allow: '/',
      },
    ],
    sitemap: 'https://www.superfaninfo.com/sitemap.xml',
  }
}
```

This allows ALL crawlers including AI bots (GPTBot, ClaudeBot, PerplexityBot, GoogleOther) per PITFALLS.md recommendation. Do NOT add any Disallow rules for AI crawlers.

**3. Create `src/lib/seo.ts`** -- Shared metadata builder for all pages:

```typescript
import type { Metadata } from 'next'
import type { Locale } from '@/lib/content/schemas'

const SITE_URL = 'https://www.superfaninfo.com'
const SITE_NAME_ES = 'SuperFan Mundial 2026'
const SITE_NAME_EN = 'SuperFan World Cup 2026'

interface PageMetadataInput {
  title: string           // The page-specific title (without site suffix)
  description: string     // 120-155 character description
  lang: Locale
  path: string            // e.g., '/es/ciudades/ciudad-de-mexico'
  alternates: {
    languages: Record<string, string>
  }
  imageAlt?: string       // Alt text for OG image
}

export function buildPageMetadata(input: PageMetadataInput): Metadata {
  const { title, description, lang, path, alternates, imageAlt } = input
  const siteName = lang === 'es' ? SITE_NAME_ES : SITE_NAME_EN
  const canonicalUrl = `${SITE_URL}${path}`

  // Truncate description to 155 chars if needed (per D-20)
  const safeDescription = description.length > 155
    ? description.slice(0, 152) + '...'
    : description

  return {
    title,  // Will use the template from layout: "{title} | SuperFan Mundial 2026" (per D-19)
    description: safeDescription,
    alternates: {
      canonical: canonicalUrl,
      languages: alternates.languages,
    },
    openGraph: {
      title: `${title} | ${siteName}`,
      description: safeDescription,
      url: canonicalUrl,
      siteName,
      locale: lang === 'es' ? 'es_419' : 'en_US',
      type: 'website',
    },
    twitter: {
      card: 'summary_large_image',
      title: `${title} | ${siteName}`,
      description: safeDescription,
    },
  }
}
```

The function handles:
- Canonical URL construction (per SEO-02)
- Open Graph tags with locale-specific site name (per SEO-02)
- Twitter Cards with summary_large_image (per SEO-02)
- Description truncation to 155 chars (per D-20)
- Title uses the template from layout.tsx so format is "{Page Title} | SuperFan Mundial 2026" (per D-19)
  </action>
  <verify>
    <automated>cd /Users/vvazquez/Desktop/Proyectos/superfan && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - src/app/sitemap.ts exists and imports getCities, getStadiums, getTeams
    - sitemap.ts includes alternates with 'es-419' and 'en' for each entry
    - sitemap.ts uses priority levels: 1.0 homepage, 0.9 cities, 0.8 stadiums, 0.7 teams
    - src/app/robots.ts exists with userAgent '*' and allow '/'
    - robots.ts references sitemap at 'https://www.superfaninfo.com/sitemap.xml'
    - robots.ts does NOT disallow any AI crawlers
    - src/lib/seo.ts exports buildPageMetadata function
    - seo.ts produces openGraph and twitter properties
    - TypeScript compilation passes with no errors
  </acceptance_criteria>
  <done>sitemap.ts generates entries for all pages with hreflang alternates and priority levels. robots.ts allows all crawlers and references sitemap. seo.ts helper produces canonical, OG, and Twitter Card metadata. TypeScript compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Update all existing pages to use buildPageMetadata for enhanced metadata</name>
  <files>
    src/app/[lang]/page.tsx,
    src/app/[lang]/ciudades/[slug]/page.tsx,
    src/app/[lang]/estadios/[slug]/page.tsx,
    src/app/[lang]/equipos/[slug]/page.tsx
  </files>
  <read_first>
    src/lib/seo.ts,
    src/app/[lang]/page.tsx,
    src/app/[lang]/ciudades/[slug]/page.tsx,
    src/app/[lang]/estadios/[slug]/page.tsx,
    src/app/[lang]/equipos/[slug]/page.tsx,
    src/lib/i18n.ts,
    src/app/[lang]/dictionaries.ts
  </read_first>
  <action>
Update `generateMetadata` in all 4 existing pages to use `buildPageMetadata` from `src/lib/seo.ts` for consistent canonical URLs, OG tags, and Twitter Cards.

**1. Update `src/app/[lang]/page.tsx`** (homepage):

Add `generateMetadata` export (currently missing on homepage):
```typescript
import type { Metadata } from 'next'
import { buildPageMetadata } from '@/lib/seo'
import { buildHomeAlternates } from '@/lib/i18n'

export async function generateMetadata({
  params,
}: {
  params: Promise<{ lang: string }>
}): Promise<Metadata> {
  const { lang } = await params
  if (!hasLocale(lang)) return {}
  const dict = await getDictionary(lang)

  return buildPageMetadata({
    title: dict.home.title,
    description: dict.home.description,
    lang: lang as Locale,
    path: `/${lang}`,
    alternates: buildHomeAlternates(),
  })
}
```

Add import for `Locale` type from `@/lib/content/schemas` and `Metadata` from `next`.

**2. Update `src/app/[lang]/ciudades/[slug]/page.tsx`**:

Replace the existing `generateMetadata` with one using `buildPageMetadata`:
```typescript
import { buildPageMetadata } from '@/lib/seo'

export async function generateMetadata({ params }): Promise<Metadata> {
  const { lang, slug } = await params
  if (!hasLocale(lang)) return {}
  const city = getCity(slug, lang as Locale)
  if (!city) return {}

  const section = lang === 'es' ? 'ciudades' : 'cities'
  return buildPageMetadata({
    title: city.name[lang as Locale],
    description: city.description[lang as Locale],
    lang: lang as Locale,
    path: `/${lang}/${section}/${slug}`,
    alternates: buildAlternates('ciudades', city.slugs),
  })
}
```

**3. Update `src/app/[lang]/estadios/[slug]/page.tsx`**:

Same pattern as cities but using `getStadium` and `'estadios'` section:
```typescript
import { buildPageMetadata } from '@/lib/seo'

export async function generateMetadata({ params }): Promise<Metadata> {
  const { lang, slug } = await params
  if (!hasLocale(lang)) return {}
  const stadium = getStadium(slug, lang as Locale)
  if (!stadium) return {}

  const section = lang === 'es' ? 'estadios' : 'stadiums'
  return buildPageMetadata({
    title: stadium.name[lang as Locale],
    description: stadium.description[lang as Locale],
    lang: lang as Locale,
    path: `/${lang}/${section}/${slug}`,
    alternates: buildAlternates('estadios', stadium.slugs),
  })
}
```

**4. Update `src/app/[lang]/equipos/[slug]/page.tsx`**:

Same pattern using `getTeam` and `'equipos'` section:
```typescript
import { buildPageMetadata } from '@/lib/seo'

export async function generateMetadata({ params }): Promise<Metadata> {
  const { lang, slug } = await params
  if (!hasLocale(lang)) return {}
  const team = getTeam(slug, lang as Locale)
  if (!team) return {}

  const section = lang === 'es' ? 'equipos' : 'teams'
  return buildPageMetadata({
    title: team.name[lang as Locale],
    description: team.description[lang as Locale],
    lang: lang as Locale,
    path: `/${lang}/${section}/${slug}`,
    alternates: buildAlternates('equipos', team.slugs),
  })
}
```

**Critical notes for all pages:**
- Keep ALL existing functionality (generateStaticParams, page rendering) unchanged
- Only modify the generateMetadata function and its imports
- The title template `%s | SuperFan Mundial 2026` is already set in layout.tsx -- do NOT duplicate the site name in the title string passed to buildPageMetadata. Pass just the page-specific title (e.g., "Ciudad de Mexico", not "Ciudad de Mexico | SuperFan Mundial 2026")
- The `buildPageMetadata` function already handles truncation and OG/Twitter generation

After updating all pages, run:
```bash
npm run build
```

Verify the build succeeds and all static pages generate correctly.
  </action>
  <verify>
    <automated>cd /Users/vvazquez/Desktop/Proyectos/superfan && npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - All 4 page files import buildPageMetadata from '@/lib/seo'
    - Homepage (page.tsx) has generateMetadata export
    - City page generateMetadata calls buildPageMetadata with correct path
    - Stadium page generateMetadata calls buildPageMetadata with correct path
    - Team page generateMetadata calls buildPageMetadata with correct path
    - `npm run build` succeeds with zero errors
    - Build output generates sitemap.xml and robots.txt routes
  </acceptance_criteria>
  <done>All existing pages use buildPageMetadata for consistent canonical URLs, OG tags, and Twitter Cards. Sitemap.xml and robots.txt are generated at build time. Build passes. Every page has unique title, description, canonical, OG, and Twitter Card metadata.</done>
</task>

</tasks>

<verification>
- `npm run build` passes with zero errors
- `grep -r "buildPageMetadata" src/app/` shows 4+ matches (all page files)
- `grep -r "openGraph" src/lib/seo.ts` confirms OG tag generation
- `grep -r "twitter" src/lib/seo.ts` confirms Twitter Card generation
- `grep -r "priority" src/app/sitemap.ts` confirms priority levels set
- `grep -r "GPTBot\|ClaudeBot\|PerplexityBot\|Disallow" src/app/robots.ts` does NOT match (no AI crawler blocks)
- Sitemap contains entries for homepages, cities, stadiums, and teams with hreflang alternates
</verification>

<success_criteria>
/sitemap.xml returns a valid sitemap with all pages at correct priority levels and hreflang alternates. /robots.txt allows all crawlers including AI bots. Every page has canonical URL, Open Graph, and Twitter Card metadata via the shared buildPageMetadata helper. Build completes successfully.
</success_criteria>

<output>
After completion, create `.planning/phases/02-base-layout-technical-seo/02-02-SUMMARY.md`
</output>
